en:
  views:
    settings:
      index:
        title: 'Settings'
        overview:
          title: 'Overview'
          account: 'Your are logged in as %{email}.'
          account_edit_link: 'Change credentials'
        conflict_handler:
          title: 'Conflict Handler'
          heuristics:
            title: 'Heuristics'
            intro: 'Defines how the conflict resolver should handle conflicts.'
            whitelist_after_n_days:
              before: 'Whitelist conflicts after'
              after: 'days'
        own_prefixes:
          title: 'My prefixes'
          intro: 'Set the prefixes of your own networks here to highlight them in the tables.'
          button: 'Add own prefix'
          success: 'Own prefix addded'
